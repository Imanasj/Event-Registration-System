# Event-Registration-System
A database system that manages college event registrations. Students register for campus events, the system enforces capacity limits, manages waitlists automatically, and provides reporting on event attendance and student engagement.
